import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "CRAFT Family Support Workshop | Evidence-Based Family Programming for Treatment Centers",
  description:
    "Add a clinically proven, turnkey family support program to your treatment center. CRAFT is 70% effective at engaging loved ones into treatment — fully facilitated for your facility.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>{children}</body>
    </html>
  );
}
